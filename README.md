# 🌤️ 智慧天氣分析系統

即時天氣查詢與 AI 智慧分析系統，支援台灣 12 個主要城市。結合 OpenWeatherMap 天氣資料與 OpenAI GPT 智慧分析，提供天氣預報、活動建議、穿搭建議及健康提醒。

> **無 OpenAI Key 也能使用** — 系統內建規則引擎，提供基礎天氣分析；有 Key 則自動升級為 GPT 深度分析。

## ✨ 功能特色

| 功能 | 說明 |
|------|------|
| 📍 即時天氣 | 溫度、體感溫度、濕度、風速、雲量、氣壓、日出日落 |
| 📊 5天預報 | 互動式 Plotly 圖表：溫度趨勢、降雨機率、濕度、風速 |
| 📅 每日預報 | 卡片式 5 日天氣總覽 |
| 🤖 AI 智慧分析 | GPT 深度分析（有 Key）/ 規則引擎基礎分析（無 Key） |
| 🎯 活動建議 | 根據氣象條件推薦戶外或室內活動 |
| 👔 穿搭建議 | 依溫度、濕度、降雨給出穿搭組合 |
| 💪 健康建議 | 運動時間、飲食、特殊族群（長者/幼童/過敏）提醒 |
| 📥 報告下載 | 可匯出完整分析報告 TXT 檔 |

### 支援城市

台北、新北、桃園、台中、台南、高雄、基隆、新竹、嘉義、宜蘭、花蓮、台東

## 🚀 快速開始

### 前置需求

- Python 3.10+
- [OpenWeatherMap API Key](https://openweathermap.org/api)（免費）
- [OpenAI API Key](https://platform.openai.com/api-keys)（可選，用於 GPT 深度分析）

### 本地開發（使用 uv）

```bash
# 1. 複製專案
git clone https://github.com/lovexyz520/weather-analysis-system.git
cd weather-analysis-system

# 2. 安裝依賴
uv sync

# 3. 設定環境變數
cp .env.example .env
# 編輯 .env，填入你的 API Key

# 4. 啟動應用程式
uv run streamlit run src/weather_analysis/app.py
```

### Streamlit Cloud 部署

1. Fork 本專案到你的 GitHub
2. 前往 [Streamlit Cloud](https://share.streamlit.io/) 建立新應用
3. 設定：
   - **Main file path**: `src/weather_analysis/app.py`
   - **Settings > Secrets** 中填入：
     ```toml
     OPENWEATHER_API_KEY = "你的金鑰"
     OPENAI_API_KEY = "你的金鑰"  # 可選
     ```

## 📁 專案結構

```
weather-analysis-system/
├── pyproject.toml              # 專案設定（uv / hatchling）
├── requirements.txt            # Streamlit Cloud 依賴
├── .env.example                # 環境變數範例
├── .gitignore
├── .streamlit/
│   └── secrets.toml.example    # Streamlit Cloud secrets 範例
├── README.md
└── src/
    └── weather_analysis/
        ├── __init__.py
        ├── app.py              # Streamlit 主程式
        ├── config.py           # 設定檔（API Key 載入邏輯）
        ├── weather_api.py      # OpenWeatherMap API 整合
        ├── visualization.py    # Plotly 圖表生成
        └── ai_analyzer.py      # AI 分析（GPT + 規則引擎 fallback）
```

## 🔑 API Key 管理

系統支援三種 API Key 來源，**優先順序由高到低**：

| 優先順序 | 來源 | 說明 |
|----------|------|------|
| 1 | 側邊欄輸入 | 使用者在 UI 即時輸入（不存檔） |
| 2 | `.env` 檔案 | 本地開發用，`config.py` 自動載入 |
| 3 | `st.secrets` | Streamlit Cloud 部署用 |

- 側邊欄輸入欄位**永遠空白**，不會顯示 `.env` 中的金鑰值
- 偵測到環境變數時僅顯示「✅ 已從環境變數載入」狀態提示
- 使用者額外輸入的 Key 會覆蓋環境變數

## 📊 AI 分析模式

| 模式 | 條件 | 說明 |
|------|------|------|
| 🤖 GPT 深度分析 | 有 OpenAI API Key | 呼叫 GPT-4o-mini 產生個人化分析 |
| 📊 基礎規則分析 | 無 OpenAI API Key | 使用內建規則引擎，依閾值產生分析 |
| ⚠️ 自動 Fallback | GPT 呼叫失敗 | 自動切換為規則引擎，不中斷體驗 |

## 🔧 技術棧

- **套件管理**: [uv](https://docs.astral.sh/uv/) + hatchling
- **前端框架**: Streamlit
- **資料處理**: Pandas
- **視覺化**: Plotly
- **AI 分析**: OpenAI API (GPT-4o-mini)
- **環境管理**: python-dotenv

## 🐛 常見問題

### 顯示「請在側邊欄輸入 API Key」
- 確認 `.env` 中的 `OPENWEATHER_API_KEY` 已正確設定
- 或在側邊欄直接輸入 API Key
- 新申請的 OpenWeatherMap Key 可能需要 2-3 小時啟用

### AI 分析顯示「基礎規則分析」
- 這是正常行為，代表未偵測到 OpenAI API Key
- 如需 GPT 深度分析，請設定 `OPENAI_API_KEY`

### Streamlit Cloud 部署失敗
- 確認 `requirements.txt` 存在於專案根目錄
- 確認 Main file path 設定為 `src/weather_analysis/app.py`
- 在 Settings > Secrets 中正確設定 API Key

## 👨‍💻 開發者

William Cheng

## 📄 授權

MIT License

---

**版本**: 1.0.0
**更新日期**: 2026 年 2 月
